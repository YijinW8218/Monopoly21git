package view;

import javax.swing.*;
import java.awt.*;

public class MonopolyBoard extends JPanel {
    private void drawBoard(Graphics20 g2){

        for(int i =0;i<40;i++){
            Rectangle r = tileBounds(i, int x0, int y0, tileSize);
            Tile t  = tiles.get(i);


        }
    }
    private Rectangle tileBounds(int index, intx0, int y0, int tileAize) {
        g2.setFront()


    }
}
